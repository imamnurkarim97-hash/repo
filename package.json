{
  "name": "fullstack-app",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build",
    "backend": "cd backend && npm install",
    "postinstall": "npm run backend && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
